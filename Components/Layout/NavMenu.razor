@namespace Coursework.Components

<div class="nav-menu">
    <nav>
        <ul>
            <!-- Link to the home page -->
            <li><NavLink href="/" class="nav-link">Back</NavLink></li>
        </ul>
    </nav>
</div>
<style>
    .nav-menu ul {
        list-style-type: none; /* Removes list dots */
        padding: 0; /* Removes default padding */
        margin: 0; /* Removes default margin */
    }

    .nav-menu li {
        margin: 0.5rem 0; /* Adds spacing between items */
       
    }

    .nav-menu .nav-link {
        text-decoration: none; /* Removes underline */
        color: green;
        padding: 0.5rem 1rem;
        border-radius: 4px;
        transition: background-color 0.3s ease, color 0.3s ease;
        font-size:26px;
    }

        .nav-menu .nav-link:hover {
            background-color: #0078d4; /* Highlight color */
            color: white; /* Hover text color */
        }

</style>
